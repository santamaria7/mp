import { Loading } from "../design";
import React from "react";

const LoadingIcon = ()=>{
    return <Loading>
        <span />
        <span />
        <span />
    </Loading>
}

export default LoadingIcon;